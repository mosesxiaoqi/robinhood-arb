# Offline example. Replace the endpoint and select explicit research parameters before collection.
rpc_url = "https://rpc.invalid"
chain_id = 4663
quote_asset = "0x0000000000000000000000000000000000000000" # native ETH
amounts = ["1000000000000000"]
min_depth = "1000000000000000000"
min_profit = "0"
confirmations = 2
requests_per_second = 2
max_concurrency = 1
retry_limit = 2
timeout_ms = 10000
max_response_bytes = 16777216
queue_capacity = 8
database = "data/research.db"
window_seconds = 60
disk_budget_bytes = 1073741824
disk_reserve_bytes = 10485760

# Optional runtime controls. Omit start_block to begin at the confirmed head on a new database.
# start_block = 56830269
# run_id = "research-window-1" # reuse to resume; use a new id for a fresh pool universe
# additional_cost = "1000000000000" # total extra cost estimate in quote-asset units; absent = unknown
